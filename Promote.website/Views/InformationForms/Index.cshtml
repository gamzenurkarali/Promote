@model IEnumerable<Promote.website.Models.InformationForm>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmailAdress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmailAdress)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InformationFormId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InformationFormId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InformationFormId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
